schema_version: "1.0"

repository:
  name: "{{PROJECT_NAME}}"
  type: "engine"
  role: "{{PLATFORM_ROLE}}"

jam:
  manual_version: "1.3.0"
  conformance_version: "1.0"

scaffold:
  template: "python-engine"
  version: "1.0.0"

python:
  package: "{{PACKAGE_NAME}}"
